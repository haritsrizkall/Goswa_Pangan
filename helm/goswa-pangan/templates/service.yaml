apiVersion: v1
kind: Service
metadata:
  name: {{ include "goswa-pangan.fullname" . }}
  labels:
    {{- include "goswa-pangan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goswa-pangan.selectorLabels" . | nindent 4 }}
